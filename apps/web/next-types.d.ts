/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Stable framework declarations for fresh-clone typechecks. Next.js owns and
// rewrites next-env.d.ts at runtime, so that generated file is intentionally
// ignored rather than committed.
